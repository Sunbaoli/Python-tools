import argparse

parser = argparse.ArgumentParser()
#parser = argparse.ArgumentParser(description='*********************')
parser.add_argument()
#parser.add_argument('--factor', type=int, default=4, help='number of upscale')
#parser.add_argument('--taset_dir', type=str, default='./data/', help='**********')

opt = parser.parse_args()
print(opt)

